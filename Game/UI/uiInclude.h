// Includes structure declarations that are often needed by different ui modules.

#ifndef UIINCLUDE_H
#define UIINCLUDE_H

typedef struct CBox CBox;
typedef struct AtlasTex AtlasTex;
typedef struct Packet Packet;
typedef struct Entity Entity;


#endif