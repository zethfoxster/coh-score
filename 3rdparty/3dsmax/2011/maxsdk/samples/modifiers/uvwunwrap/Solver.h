
#ifndef __SOLVER__H
#define __SOLVER__H
/*
#include "TVData.h"

class EdgeBondage
{
public:
	float dist;
	float str;
	float distPer;
	int v1,v2;
	int isEdge;
	Point3 edgePos; // if this is an edge spring this is locked point connected to v1
	Point3 originalVertPos; // this is the original pos of V1 so we can quickly recalc our distances
	int flags;
};


class SpringClass
{
public:
	Point3 vel, pos;
	Point3 tempVel[6];
	Point3 tempPos[6];
	float mass;
};

class EdgeBondage
{
public:
	float dist;
	float str;
	float distPer;
	int v1,v2;
	BOOL isEdge;
	Point3 edgePos; // if this is an edge spring this is locked point connected to v1
	Point3 originalVertPos; // this is the original pos of V1 so we can quickly recalc our distances
	int flags;
};

class SpringClass
{
public:
	Point3 vel, pos;
	Point3 tempVel[6];
	Point3 tempPos[6];
	float mass;
};

class Solver
{

public:
	
	void Solve(int startFrame, int endFrame, int samples,
			   Tab<EdgeBondage> &springs, Tab<SpringClass> vertexData, float decay);

private:

};

*/
#endif